// It won't work here
// If you're working in a Node.js environment or another environment where the DOM is not available,
//  you won't be able to access the document object directly.

// If you want to execute this code in a browser environment, make sure you run it within an HTML file or in a browser console.

// you can simulate a DOM environment in Node.js.
// jsdom is a JavaScript implementation of the DOM and HTML standards that allows you to parse and manipulate HTML documents in Node.js.

let btn = document.getElementById("btn1");
console.log(btn);
